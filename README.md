# MyAnimeList iOS クライアント (WKWebView でサンプル)

## ビルド

web のビルド

```sh
cd mal-client/mal-client/web:

npm install
npm start
```

設定ファイルの作成
```sh
cd mal-client
cp config.js.template config.js
edit config.js
```
